public class Neuron
{


    //public void Think(TentacleBrain brain)
    //{
    //    if (!brain.IsAlive.Value) brain.StartImpulse(brain.DespawnImpulses[0].Value.Go());
    //    while (DoingAction) yield return null;
    //    if (IsAlive.Value)
    //    {
    //        if (target.Value == null) StartCoroutine(DoingActionWrapper(SearchImpulses[0].Value.Go()));
    //        while (DoingAction) yield return null;
    //        if (target.Value == null) BrainDead();
    //        while (DoingAction) yield return null;
    //        if (IsAlive.Value)
    //        {
    //            if (target.Value != null) StartCoroutine(DoingActionWrapper(AttackImpulses[0].Value.Go()));
    //            while (DoingAction) yield return null;
    //        }
    //    }
    //    DoingActions = false;
    //}



}
